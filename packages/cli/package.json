{
  "name": "@umbra-protocol/cli",
  "version": "0.1.0-alpha.1",
  "description": "Umbra Protocol CLI — Register organizations and manage soulbound credentials from the command line",
  "type": "module",
  "bin": {
    "umbra-protocol": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/umbra-protocol/umbra",
    "directory": "packages/cli"
  },
  "keywords": ["umbra", "soulbound", "credentials", "cardano", "midnight", "cli"],
  "files": ["dist", "package.json", "README.md"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@adasouls/soulbound-core": "*",
    "@adasouls/soulbound-sdk": "*",
    "@meshsdk/core": ">=1.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0"
  }
}
